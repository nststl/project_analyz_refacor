# Legacy: OMS (SQLite + MongoDB)

Тут збережено попередній навчальний приклад **ONE Order / PNR** (`oms_app.py`, `seed_db.py`, `bulk_migrate.py`, тести OMS).

Він **не є** основною реалізацією поточної курсової (бібліотека, in-memory, без зовнішніх БД). Для здачі використовуйте код у `src/` та `tests/`.
